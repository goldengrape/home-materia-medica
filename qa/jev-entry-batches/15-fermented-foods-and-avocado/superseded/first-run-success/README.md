# First successful run — superseded after citation audit

This archive preserves the outputs of the first successful batch-15 run (GitHub Actions run 36018666716; Jev actual model jev-1.13.0). A later citation audit revised the kimchi summary wording and split tempeh-gembus evidence into separately cited studies. The old kimchi native result used summary SHA-256 `4e8e103b6fcbc538fd690d9ec064ff9a70fe4f28f23ad94ad24f4cc84d569c9d`; it is preserved for audit and was intentionally moved out of the active raw directory so the revised frozen summary can receive a matching Jev result.

The original run's Actions artifacts were stage-1/2 artifact 10816260093 and native Jev artifact 10815098771. They provide an additional immutable copy while their retention window remains active.

Files below preserve the prior kimchi summary, freeze snapshot, Jev JSON, Mapping, reader entry, prior tempeh research dossier, batch config, QA logs, and checksums.
